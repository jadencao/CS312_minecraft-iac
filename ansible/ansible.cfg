# ----------------------------------------------------------------------------
# ansible.cfg — Ansible settings for this project. Ansible automatically reads
# this file when commands are run from this directory.
# ----------------------------------------------------------------------------

[defaults]
inventory = inventory.ini
host_key_checking = False